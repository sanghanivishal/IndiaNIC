import React from 'react';
import {Modules} from "./Actions";
import Actions from "../../internal/modules/Actions";

export default {
  [Modules.GET_JOB_DETAILS]: ({ dispatch, payload, appState }) => {
    // dispatch(Actions.common.navigate("DashboardStack"))
  },
}

